import { MicroDirective } from './micro.directive';

describe('MicroDirective', () => {
  it('should create an instance', () => {
    // const directive = new MicroDirective();
/*     expect(directive).toBeTruthy(); */
  });
});
